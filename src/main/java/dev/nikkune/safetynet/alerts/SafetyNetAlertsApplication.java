package dev.nikkune.safetynet.alerts;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * The SafetyNetAlertsApplication class serves as the entry point for the SafetyNet Alerts Spring Boot application.
 * <p>
 * It contains the main method which launches the application.
 */
@SpringBootApplication
public class SafetyNetAlertsApplication {

    public static void main(String[] args) {
        SpringApplication.run(SafetyNetAlertsApplication.class, args);
    }

}
